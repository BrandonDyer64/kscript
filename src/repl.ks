
; let readline = require('readline')
; let parse = require('../lib/index').parse

; let rl = readline.createInterface(obj{
  input: process.stdin;
  output: process.stdout;
})

; let start = {
  ; rl.question('> ', (answer) => {
    < answer + '\n'
    < _.split('\n')
    ; _.shift()
    < _.join('\n')
    parse
    eval
    > console.log
    start
  })
}

start
